<!-- Why did the below work when using :venue -->
<!-- What am I passing below? -->
<!-- Why would I want to pass an instance variable or array as a param or argument? -->

<%= form_for([@venue, @event])  do |f| %>

  <%= f.label :date %>
  <%= f.text_field :date %>

  <%= f.label :time %>
  <%= f.text_field :time %>

  <%= f.label :band_id %>
  <%= f.text_field :band_id %>

  <%= f.submit "Book" %>

<% end %>
